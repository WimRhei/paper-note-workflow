# 论文笔记 Markdown 模板

这份模板用于生成最终 Obsidian 论文笔记。它和 `paper-note-schema.md` 的关系是：

- `paper-note-schema.md` 规定阅读和判断准则。
- 本文件规定笔记落地时的章节骨架、文件命名、图表写法和可复查信息位置。

生成笔记时，不要机械填空；如果某节对论文不适用，可以删掉或合并。图表文件规则必须保留；复现实用信息默认压缩；相关工作定位只有在影响理解技术差异时才单独写，否则合并到背景与问题。

## 文件命名

- 笔记文件名使用论文提出的核心概念、系统、方法或 artifact 名称：`xxx.md`。
- `xxx` 使用简短英文或论文原名缩写，避免空格；必要时用 `-` 连接。
- 图表文件必须使用同一前缀：`xxx-1.ext`、`xxx-2.ext`、`xxx-3.ext`。
- 为兼容本地 Obsidian `paper-archiver` 插件，最终落盘必须是 `03-论文阅读/Inbox/xxx/xxx.md`。
- 图表文件放在 `03-论文阅读/Inbox/xxx/Figure/`，源 PDF 直接放在 `03-论文阅读/Inbox/xxx/`，通常命名为 `xxx.pdf`。
- 读后修订时可以临时保留 `xxx-edit.md`，但最终归档版仍必须覆盖回 `xxx.md`。
- 抽取文本、figure metadata、未嵌入图表等中间文件可以保留给后续阅读/AI 检查；归档插件可能清理未引用的非 PDF 文件，不要在模板里引用它们。

## 图表文件规则

图表文件规则是强约束，不要按个人习惯改写。

### 命名

- 所有嵌入笔记的图表文件都放在笔记相邻的 `Figure/` 目录中。
- 文件名格式必须是：`xxx-N.ext`。
- `xxx` 必须和笔记文件名完全一致。
- `N` 从 1 开始，按图表在笔记中首次出现的顺序递增。
- `ext` 保留实际文件格式，例如 `png`、`jpg`、`webp`。
- 不允许使用原始文件名，例如 `Figure_1.png`、`table-2.png`、`image.png`。
- 不允许跳号、重号或按论文原 Figure/Table 编号命名。

### 嵌入格式

每张图表必须就地嵌入到它支撑的段落附近。推荐格式：

```markdown
![Figure 2: 原论文 caption 的压缩版](Figure/xxx-1.png)

这张图回答的问题是：...
它支撑的判断是：...
```

不要使用 `attachments/`。`paper-archiver` 归档后会把图片移动到主题目录的 `Figure/` 下，因此正文必须使用 `Figure/xxx-N.ext` 这样的相对路径。

### 原编号保留

- Markdown alt 文本必须保留原论文编号，例如 `Figure 1`、`Fig. 3`、`Table 2`。
- 文件名中的编号只表示笔记出现顺序，不表示原论文编号。
- 正文说明中要写清该图/表回答的问题，不能只复述 caption。

### 缺失图表

如果 `pdffigures2` 没提取出关键图表，必须在应嵌入的位置写：

```markdown
FIX: 手工嵌入 Figure/Table N
```

不要把所有缺失图表统一放到文末处理。

## 模板正文

```markdown
# xxx

## 论文信息

- 标题：
- 作者与机构：
- 年份与会议/期刊/ArXiv：
- 链接：

> [!Key]
> 用 2-4 句话写清楚：这篇论文解决什么问题，真实瓶颈在哪里，算法/系统分别改了什么，以及 trade-off 为什么变好。

## 背景与问题

写清楚研究对象、目标指标、原方法/系统如何运行，以及真实瓶颈在哪里。只写理解机制所需的 workflow，不要登记所有背景信息。

必要时用 3-7 步描述原 workflow：

1. ...
2. ...
3. ...

瓶颈落在第 X 步：...

如果有关键问题图，在这里就地嵌入：

![Figure N: caption 压缩版](Figure/xxx-1.png)

这张图回答的问题是：...
它支撑的判断是：...

## 方法主线

先用几句话说明这篇论文采用哪种组织逻辑。选择下面一种骨架，删掉另一种；如果都不适合，自定义更短结构。

### 选项 A：Layered Co-Design

适用于多个协同层级。层级名称必须来自论文真实机制，不预设应用/算法/硬件。

#### 关键 Observation / Insight

只保留能推出设计的观察：

- O1：...
- O2：...

#### 分层设计

Layer 1：
- 这一层的问题：
- 机制：
- 输出/约束：
- 对下一层的影响：

Layer 2：
- ...

#### 跨层协作

写清楚上层给下层什么信息/约束，下层提供什么能力/反馈。

### 选项 B：Algorithm-to-Hardware Co-Design

适用于先解决算法/方法层问题，再设计硬件/系统支撑，让收益落到端到端指标的论文。

#### 算法层

写清楚算法自身的问题、算法解法、改完得到什么可执行性质：

- 问题：
- 解法：
- 为什么成立：
- 代价/边界：

#### 硬件 / 系统层

先讲静态组成，再讲连接关系和动态数据流。只有机制需要时才写初始化和状态更新。

```text
静态组成：模块、存储、控制器、互联
连接关系：谁给谁数据，谁控制谁
动态数据流：一次 token/tile/request/instruction 如何流过
调度关系：哪些阶段顺序、并行、流水重叠或被隐藏
存储与状态：什么数据放哪里，容量/entry/bit width 是多少
```

如果机制抽象，用一个最小例子走通：

```text
Example:
...
```

#### 算法与硬件如何接上

写清算法输出了什么性质，硬件/系统如何利用这些性质。

### 关键机制图

在对应机制处就地嵌入，不要集中堆图：

![Figure N: caption 压缩版](Figure/xxx-2.png)

这张图回答的问题是：...
它支撑的判断是：...

## 实验设置与复现实用信息

默认压缩到 5-7 行；只有复现细节本身很重要时再展开。

- 数据集 / benchmark：
- workload / trace：
- baseline：
- 硬件平台：
- 仿真器 / 编译器 / 软件栈：
- 训练或推理设置：
- 主要指标：
- 关键缺口：`FIX: info`

## 实验结果

每个实验小段形成：结论 -> 图/表 -> 读图说明 -> 关键数字 -> 这验证了技术链路中的哪一段。

### 主结果

![Figure/Table N: caption 压缩版](Figure/xxx-3.png)

这张图/表回答的问题是：...
关键数字是：...
这说明：...

### 消融 / 开销 / 泛化

...

## 缺点不足与适用边界

- 哪些条件下会失效。
- 依赖哪些强假设。
- 参数、阈值、模型、硬件或 workload 换了以后是否还成立。
- 硬件/系统实现有没有只停留在仿真或模块验证。

## 总结思考

- 可迁移判断：
- 以后遇到类似问题先看哪里：
```