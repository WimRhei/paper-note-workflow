interface:
  display_name: "Paper Note Drafter"
  short_description: "Draft using the user-selected reading route"
  default_prompt: "Use $paper-note-drafter with the reading route already recorded in route.txt, or with the explicit Algorithm or Architecture route I provide for a bare PDF. Never infer it from the paper."
